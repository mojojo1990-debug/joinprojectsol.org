const menuButton=document.querySelector('.menu-button');const nav=document.querySelector('.nav');const form=document.querySelector('#builderSearch');const input=document.querySelector('#ideaInput');const response=document.querySelector('#builderResponse');document.querySelectorAll('[data-idea]').forEach(button=>button.addEventListener('click',()=>{input.value=button.dataset.idea;input.focus()}));menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});form.addEventListener('submit',event=>{event.preventDefault();const idea=input.value.trim();if(!idea){response.textContent='Tell us what you are curious about. No idea is too small.';input.focus();return}response.textContent=`Let’s build “${idea}” together. Your first step is to describe what success looks like.`});document.querySelector('#year').textContent=new Date().getFullYear();
