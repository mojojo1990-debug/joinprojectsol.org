# Project Sol Website

Upload `index.html`, `styles.css`, `script.js`, and `CNAME` to the root of the `main` branch in the `joinprojectsol.org` GitHub repository.

GitHub Pages settings:
- Branch: `main`
- Folder: `/ (root)`
- Custom domain: `joinprojectsol.org`
